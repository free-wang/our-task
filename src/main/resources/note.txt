这是一个笔记，用来在编码中遇到的问题
-------------------------------------------------------------------

2.Thymeleaf中的三元运算符：<p th:text="${user.online ? (user.vip ? 'VIP用户在线' : '普通用户在线') : '离线'}"></p>

dr



-------------------------------------------问题待办---------------------------------------------
1.配置了Thymeleaf之后，使用Swagger，会报错找不到模板解析器，但是数据操作是对的。后面在前端页面进行操作，
  就不会提示这个错误了,这个后面再处理